services:
  py--datascience:
    container_name: py--datascience
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    platform: linux/arm64
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
