services:
  py--huggingface:
    container_name: py--huggingface
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    platform: linux/arm64
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
